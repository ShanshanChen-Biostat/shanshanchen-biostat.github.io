### Handling Time Series in R

** For intensive longitudinal data (e.g. with a sampling rate more than 1/15 Hz), I recommend moving to Matlab for data visualization (natrually interactive plot). 
Still, it's often very useful to know how to handle time series in R too. 

```
library(chron)
library(zoo)
```
